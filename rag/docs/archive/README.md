# Archivo histórico

Documentos de trabajo (informes, planes, diagnósticos) de iteraciones ya
cerradas. Se conservan como registro de **por qué** el sistema quedó como
quedó — no describen el estado actual del código; para eso ver el
`CLAUDE.md` de la raíz del repo y `rag/docs/AGENTS.md`.

No se referencian desde ningún otro documento ni desde el código; están
aquí solo para consulta histórica.
