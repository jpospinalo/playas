# ATLAS — Sistema agéntico de apoyo para la orientación normativa y jurisprudencial sobre playas en Colombia
# OpenSpec SDD Configuration

project: ATLAS
description: Agentic RAG system for Colombian beach/coastal jurisprudence

tech_stack:
  backend: Python FastAPI
  frontend: Next.js 16 + React 19
  database: Firebase/Firestore
  vector_db: ChromaDB
  embeddings: Ollama (embeddinggemma)
  llm: OpenRouter / Gemini (fallback)

test_command: "make test"
strict_tdd: false

sdd_preferences:
  artifact_store: openspec
  review_budget_lines: 400
  pr_strategy: auto-forecast
  execution_mode: interactive
